import React from 'react';

const ProjectsPage = () => (
  <div style={{ padding: '40px' }}>
    <h1>Projects</h1>
    <p>This is a dedicated page for my projects.</p>
    <ul>
      <li><strong>Weather App</strong>: Uses OpenWeather API</li>
      <li><strong>Todo App</strong>: Local storage, clean UI</li>
      <li><strong>Blog Platform</strong>: React + Markdown</li>
    </ul>
  </div>
);

export default ProjectsPage;
